class ArtGlobeAdapter {
    
    constructor(baseURL="http://127.0.0.1:3000"){
        this.baseCampaignURL = `${baseURL}/art`
        /* when creating a new adpater object, 
        this is setting up the URL for the user */
    }

    postForm() {

    }
}